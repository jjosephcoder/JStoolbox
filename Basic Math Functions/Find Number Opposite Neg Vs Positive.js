function opposite(number) {
    let result =0; 
    if (number >0){
    result = number*(-1);
    return result;
  }else{
  result = Math.abs(number)
  
 
  return result; 
  }
  
}

function opposite(number) {
    //declare result variable
    let result =0; 
    //If number is greater than 0 hence positive multiply it by -1 to invert it in to a negative number
    if (number >0){
    result = number*(-1);
    return result;
  }else{
  else use the Match.abs function to transform negative numbers into positive integer 
  result = Math.abs(number)
  
 
  return result; 
  }
  
}