//Str is a string passed in as a parameter to the function, strings are arrays, so use slice to reconstruct remving the first and last characters.

function removeChar(str){

  var output="";  

  output = str.slice(1,[-1]);

return (output)

};
